---
import CaseStudyLayout from "../../layouts/CaseStudyLayout.astro";

const page = {
title: "Tax bill calculator",
intro: [
"A constraint-driven calculator built with exclusive Wharton model outputs to show how the final tax bill changes household finances."
],
links: [{ label: "Live project", href: "https://www.washingtonpost.com/business/interactive/2025/trump-big-beautiful-bill-your-taxes-cuts/" }],
heroImage: "/demos/tcja/10.png",
}                                                                                                                                               ;

const gridImages = [
  { src: "/demos/tcja/6.png", alt: "Profile selection" },
  { src: "/demos/tcja/7.png", alt: "Profile selection"  },
  { src: "/demos/tcja/8.png", alt: "Profile selection"  },
  { src: "/demos/tcja/9.png", alt: "Profile selection"  },

];
---

<CaseStudyLayout title={`${page.title} — Szu Yu Chen`}>
{/* HERO */}
<header class="csHero">
<div class="csHero__text">
<h1 class="csHero__title">{page.title}</h1>

{page.intro.map((p) => <p class="csHero__p">{p}</p>)}

<div class="csHero__links">
{page.links.map((l) => (
<a class="csLink" href={l.href} target="_blank" rel="noreferrer">
{l.label}
</a>
))}
</div>
</div>

<figure class="csHero__media">
<img src={page.heroImage} alt="" loading="lazy" decoding="async" />
</figure>
</header>

{/* BIG IMAGE + SIDE CAPTION */}
<section class="csBlock">
<figure class="csFrame">
<img src="/demos/tcja/2.png" alt="" loading="lazy" decoding="async" />
</figure>
<aside class="csAside">
After selecting a household profile, readers see the modeled outcome.
</aside>
</section>

{/* CENTERED IMAGE + CAPTION UNDER */}
<section class="csCenter">
<figure class="csFrame csFrame--wide">
<img src="/demos/tcja/3.png" alt="" loading="lazy" decoding="async" />
</figure>
<p class="csCaption">
Side-by-side comparisons help readers see who benefits most — and who sees gains limited by safety-net cuts.
</p>
</section>

{/* 2x3 GRID + CENTER CAPTION (NYT-like moment) */}
<section class="csGridBlock">
  <div class="csGrid csGrid--featured">
    {gridImages.map((img, i) => (
      <figure class={`csTile ${i === 0 ? "isFeatured" : ""}`} key={img.src}>
        <img src={img.src} alt={img.alt ?? ""} loading="lazy" decoding="async" />
      </figure>
    ))}
  </div>

  <p class="csCaption csCaption--center">...</p>
</section>

{/* FINAL WIDE FRAME */}
<section class="csCenter">
<figure class="csFrame csFrame--letterbox">
<img src="/demos/tcja/5.png" alt="" loading="lazy" decoding="async" />
</figure>
<p class="csCaption csCaption--center">Readers can customize key inputs through structured fields and dropdowns. Constraints keep entries within modeled ranges, avoiding combinations the data can’t support.</p>
</section>
</CaseStudyLayout>
