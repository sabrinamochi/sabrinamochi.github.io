---
import CaseStudyLayout from "../../layouts/CaseStudyLayout.astro";

const page = {
title: "Eclipse calculator",
intro: [
"We built an interactive calculator that lets readers search any U.S. city and instantly see how much of the sun will be covered, when the eclipse begins and ends, and what peak totality looks like from their location."
],
links: [{ label: "Live project", href: "https://www.washingtonpost.com/science/interactive/2024/solar-eclipse-view-cities/" }],
heroImage: "/demos/eclipse/vid.mov",
}             

const gridImages = [
{ src: "/demos/eclipse/9.png", alt: "Profile selection" },

{ src: "/demos/eclipse/4.png", alt: "Profile selection" },
{ src: "/demos/eclipse/8.png", alt: "Profile selection" },
{ src: "/demos/eclipse/7.png", alt: "Profile selection" },

]   ;                                                                                ;
---

<CaseStudyLayout title={`${page.title} — Szu Yu Chen`}>
{/* HERO */}
<header class="csHero">
<div class="csHero__text">
<h1 class="csHero__title">{page.title}</h1>

{page.intro.map((p) => <p class="csHero__p">{p}</p>)}

<div class="csHero__links">
{page.links.map((l) => (
<a class="csLink" href={l.href} target="_blank" rel="noreferrer">
{l.label}
</a>
))}
</div>
</div>

<figure class="csHero__media">
<img src={page.heroImage} alt="" loading="lazy" decoding="async" />
</figure>
</header>

{/* BIG IMAGE + SIDE CAPTION */}
<section class="csBlock">
<figure class="csFrame">
<img src="/demos/eclipse/1.png" alt="" loading="lazy" decoding="async" />
</figure>
<aside class="csAside">
A simple animated sun–moon diagram illustrates exactly how the eclipse unfolds, paired with precise local start, peak and end times.
</aside>
</section>

{/* CENTERED IMAGE + CAPTION UNDER */}
<section class="csCenter">
<figure class="csFrame csFrame--wide">
<img src="/demos/eclipse/3.png" alt="" loading="lazy" decoding="async" />
</figure>
<p class="csCaption">

Search or select any city to instantly center the map and generate a localized forecast.
</p>
</section>

{/* 2x3 GRID + CENTER CAPTION (NYT-like moment) */}
<section class="csGridBlock">
  <div class="csGrid csGrid--featured">
    {gridImages.map((img, i) => (
      <figure class={`csTile ${i === 0 ? "isFeatured" : ""}`} key={img.src}>
        <img src={img.src} alt={img.alt ?? ""} loading="lazy" decoding="async" />
      </figure>
    ))}
  </div>

  <p class="csCaption csCaption--center">...</p>
</section>


{/* FINAL WIDE FRAME */}
<section class="csCenter">
<figure class="csFrame csFrame--letterbox">
<img src="/demos/eclipse/2.png" alt="" loading="lazy" decoding="async" />
</figure>
</section>
</CaseStudyLayout>
